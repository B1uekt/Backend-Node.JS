<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="topnav">
        <a href="/">Home</a>
        <a href="/create">Create User</a>
    </div>
    <div class="form-add-new">
        <% if(!userEdit){ %>
            <p style="padding:0 100px">Không có thông tin User</p>
            <% } else { %>
                <form action="/update-user" method="POST">
                    <fieldset>

                        <legend>Edit Users</legend>

                        <input type="hidden" name="userId" value="<%= userEdit.id %>" type="text">

                        <div class="input-group">
                            <label for="">Email:</label>
                            <input id="emailId" name="email" type="text" value="<%= userEdit.email %>">
                        </div>

                        <div class="input-group">
                            <label for="">First Name:</label>
                            <input name="firstname" type="text" value="<%= userEdit.firstName %>">
                        </div>

                        <div class="input-group">
                            <label for="">Last Name:</label>
                            <input name="lastname" type="text" value="<%= userEdit.lastName %>">
                        </div>

                        <div class="input-group">
                            <label for="">Address:</label>
                            <input name="address" type="text" value="<%= userEdit.address %>">
                        </div>

                        <div class="input-group">
                            <Label>Gender:</Label>
                            <select name="gender">
                                <%= userEdit.gender %>
                                    <option value="Men" <%=userEdit.gender ? 'selected' : '' %> >Men</option>
                                    <option value="Women" <%=!userEdit.gender ? 'selected' : '' %> >Women</option>
                            </select>
                        </div>



                        <div class="input-group">
                            <label for="">Role:</label>
                            <input name="role" type="text" value="<%= userEdit.roleid %>">
                        </div>
                        <button>Save</button>
                    </fieldset>
                </form>
                <% } %>
    </div>
</body>

</html>